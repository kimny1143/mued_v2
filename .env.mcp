# MCP Server Environment Variables
# This file contains environment variables for MCP Server

# Authentication token for MCP server
PLAYWRIGHT_MCP_TOKEN=dev-token-mued-lms

# MCP Server Port
MCP_SERVER_PORT=3333

# Debug mode
MCP_DEBUG=false

# Headless mode for CI
MCP_HEADLESS=true

# Test user credentials (for development only)
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Allowed domains for testing
MCP_ALLOWED_DOMAINS=localhost:3000,dev.mued.jp,*.vercel.app